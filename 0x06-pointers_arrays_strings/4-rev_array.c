#include "main.h"
/**
 * reverse_array - reverses an array of int
 * @a: array parameter
 * @n: number of array elements
 */
void reverse_array(int *a, int n)
{
	int b, c;

	for (b = 0; b < c--; b++)
	{
		c = a[b];
		a[b] = a[n];
		a[n] = c;
	}
}
